<p id="notice"><%= notice %></p>

<h1>Invitations</h1>

<table>
  <thead>
    <tr>
      <th>User invita</th>
      <th>User invitado</th>
      <th>Fecha</th>
      <th>Local</th>
      <th>Confirma</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @invitations.each do |invitation| %>
      <tr>
        <td><%= invitation.user_invita %></td>
        <td><%= invitation.user_invitado %></td>
        <td><%= invitation.fecha %></td>
        <td><%= invitation.local_id %></td>
        <td><%= invitation.confirma %></td>
        <td><%= link_to 'Show', invitation %></td>
        <td><%= link_to 'Edit', edit_invitation_path(invitation) %></td>
        <td><%= link_to 'Destroy', invitation, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Invitation', new_invitation_path %>
