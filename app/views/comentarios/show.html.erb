<p id="notice"><%= notice %></p>

<p>
  <strong>Usuario:</strong>
  <%= @comentario.user_id %>
</p>

<p>
  <strong>Local:</strong>
  <%= @comentario.local_id %>
</p>

<p>
  <strong>Fecha de Creacion:</strong>
  <%= @comentario.fecha %>
</p>

<p>
  <strong>Contenido:</strong>
  <%= @comentario.contenido %>
</p>

<% if user_signed_in? and @comentario.user_id == current_user.id %>
  <%= link_to 'Editar Comentario', comentarios_edit_path(@comentario.id) %>
<% end %>
<%= link_to 'Eliminar Comentario', comentario_path(@comentario.id), method: :delete, alert: 'Estás seguro?' %>
<%= link_to 'Lista Comentarios', comentarios_path %>